<table>
  <tr>
  	<!-- why isn't this name showing up? -->
    <th><h2>Question Sets for <%= params[:course_name] %></h2></th> 
    
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @answers.each do |answer| %>
  <tr>
    <td><%= answer.class_date %></td>
    
    <td><%= link_to 'Remove this Question Set', answer, :method => :delete, :data => { :confirm => 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />