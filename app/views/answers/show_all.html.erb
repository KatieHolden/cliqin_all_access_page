<table>
  <tr>
  	<!-- why isn't this name showing up? -->
    <th><h2>Question Sets for <%= params[:course_name] %></h2></th> 
    
    <th></th>
    <th></th>
  </tr>

<% @answers.each do |answer| %>
  <tr>
	<td><br><%= answer.class_date.strftime("%b %-d, %Y")%></td>
    
	<td><br><%= link_to 'Remove', answer, :method => :delete,
			:data => { :confirm => 'Are you sure?' }, :class => "btn" %></td>
  </tr>
<% end %>
</table>

<br />
